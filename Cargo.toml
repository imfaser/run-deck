[workspace]
members = ["src-tauri"]  # 空项目仅需 Tauri 核心目录
resolver = "2"           # 保留 Rust 新依赖解析器

# ===== 最小化构建配置 =====
[profile.release]
panic = "unwind"
codegen-units = 1
lto = "thin"
opt-level = 3
debug = 1
strip = "none"
overflow-checks = true

[profile.dev]
incremental = true
codegen-units = 64
opt-level = 0
debug = true
strip = "none"
overflow-checks = true
lto = false

# ===== 仅保留核心依赖 =====
[workspace.dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150" }

# ===== Lint 规则 =====
[workspace.lints.clippy]
all = "warn"
pedantic = "warn"